import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NISAAP - Network Infrastructure Security Assessment & Action Platform",
  description: "Enhancing Network Security in IoT Devices at National Railways of Zimbabwe",
  keywords: ["NISAAP", "NRZ", "Network Security", "IoT", "Railway Security", "Zimbabwe"],
  authors: [{ name: "National Railways of Zimbabwe - IT Security Division" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
